services:
  gameday:
    build: .
    container_name: gameday
    ports:
      - "0.0.0.0:3000:3000"
    env_file:
      - .env
    environment:
      - DB_PATH=/app/data/gameday.db
    volumes:
      - ./data:/app/data
      - ./public/uploads:/app/public/uploads
    restart: unless-stopped
